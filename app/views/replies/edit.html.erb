<div class="section">

  <% form_for([@topic, @reply]) do |f| %>
    <%= f.error_messages %>
    <p>
      <%= f.label :body %><br />
      <%= f.text_area :body %>
    </p>
    <p>
      <%= f.submit "Update" %>
    </p>
  <% end %>

</div>